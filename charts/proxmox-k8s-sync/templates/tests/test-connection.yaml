apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "proxmox-k8s-sync.fullname" . }}-test-connection"
  labels:
    {{- include "proxmox-k8s-sync.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "proxmox-k8s-sync.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
